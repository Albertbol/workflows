name: Test
description: Flutter test
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
    - uses: subosito/flutter-action@main
      with:
        channel: "stable"
    - shell: bash
      run: |
        flutter pub get
        flutter --version
        flutter test

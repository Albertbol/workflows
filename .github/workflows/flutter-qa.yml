name: Flutter qa work flow
on:
  workflow_call:
    secrets:
      FLUTTER_PACKAGES_MONOREPO_PK:
        required: true
jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: Albertbol/workflows/.github/actions/test/flutter@main
        with:
          FLUTTER_PACKAGES_MONOREPO_PK: ${{ secrets.FLUTTER_PACKAGES_MONOREPO_PK }}
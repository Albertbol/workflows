name: Flutter qa work flow
on:
  workflow_call:
jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: Albertbol/workflows/.github/actions/analyze/flutter@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: Albertbol/workflows/.github/actions/test/flutter@main
